# 📌 Headhunter API Explorer — обзор проекта

End-to-end Data Engineering пайплайн для сбора и подготовки данных о рынке вакансий  
с акцентом на архитектуру, качество данных и compliance-ориентированные сценарии использования.

## Поток данных

**HeadHunter API → Airflow → MinIO (Bronze) → Postgres → dbt (планируется) → BI (планируется)**

Проект демонстрирует практический, приближённый к продакшену подход  
к построению data-платформ:

- слоистая архитектура  
- идемпотентные загрузки  
- наблюдаемость  
- обязательные проверки качества данных  

## Технологический стек

- Python  
- Apache Airflow  
- MinIO (S3-совместимое хранилище)  
- Postgres  
- Docker  
- dbt (планируется)  

---

## 🧩 Назначение проекта

Проект реализует Data Engineering платформу для автоматизированного:

- сбора данных о вакансиях из HeadHunter API  
- хранения raw-данных как источника истины  
- подготовки данных для аналитики и compliance-кейсов  

Помимо классической аналитики рынка труда, проект ориентирован  
на **RegTech / Compliance-сценарии**:

- анализ работодателей  
- анализ отраслей и географии  
- выявление потенциально рискованных формулировок в описаниях вакансий  

---

## 🏗 Архитектура пайплайна

```text
HeadHunter API
      ↓
Airflow (DAG-и ingestion)
      ↓
MinIO (Bronze слой, JSONL, партиционирование по дате)
      ↓
Airflow (загрузка и проверки)
      ↓
Postgres (raw / staging)
      ↓
dbt (Silver / Gold слои) — планируется
      ↓
BI / дашборды — планируется
📐 Архитектурные принципы
Source of Truth — MinIO (Bronze слой)

Raw-данные не модифицируются

Чёткое разделение:

ingestion ≠ transformations

daily-загрузки ≠ init / backfill

идемпотентные повторные запуски DAG-ов

обязательные проверки покрытия и качества данных (coverage, DQ)

⚙️ Состав локального окружения
Airflow Webserver → http://localhost:8090

Airflow Scheduler — выполнение DAG-ов

Postgres — база данных для Airflow и аналитических слоёв

Adminer → http://localhost:8091
(UI для Postgres)

MinIO → http://localhost:9001
(S3-совместимое объектное хранилище)

🚀 Установка и запуск
1. Клонирование репозитория
bash
Копировать код
git clone https://github.com/<your-username>/<your-repo>.git
cd <your-repo>
2. Настройка окружения
bash
Копировать код
cp .env.example .env
Отредактируй .env, указав свои порты и пароли.

3. Запуск контейнеров
bash
Копировать код
docker compose up --build
📂 Структура репозитория
text
Копировать код
.
├── dags/
│   ├── aida_hh_init_*.py
│   ├── aida_hh_daily_*.py
│   ├── aida_hh_details_*.py
│   ├── nataliia_hh_to_postgres.py
│   └── utils/
├── configs/
│   └── search_profiles.yaml
├── docker-compose.yml
├── Dockerfile
├── .env.example
├── logs/
└── minio_data/
📊 Слои данных
Bronze (реализовано)
Хранение raw-данных в MinIO в формате JSONL.

text
Копировать код
hh-raw/
└── bronze/
    └── hh/
        ├── vacancies_list/
        │   ├── load_type=init/
        │   └── load_type=daily/
        │       └── dt=YYYY-MM-DD/
        │           └── part-000.jsonl
        └── vacancy_details/
            └── load_type=daily/
                └── dt=YYYY-MM-DD/
                    └── batch=XXX/
                        └── part-000.jsonl
Silver / Gold — планируется (через dbt в Postgres).

🧪 Качество данных и наблюдаемость
Реализовано
контроль идемпотентности загрузок

логирование количества записей и батчей

отчёт покрытия: expected vs loaded

сбор и хранение failed-records

алерты о статусе DAG-ов (Telegram)

Планируется
расширенные DQ-проверки

dbt tests (unique, not null, relationships)

аномалии и compliance-алерты

👥 Команда и зоны ответственности
Aida Iskakova
Архитектура пайплайна (Bronze / Silver / Gold)

Ingestion: HeadHunter API → MinIO (Bronze)

Работа с API: пагинация, rate limits, retries

Идемпотентность и структура хранения

Формирование требований к данным

Compliance-логика и risk-signals

Natalia Tarasova
Загрузка данных из MinIO в Postgres (raw / staging)

dbt-модели Silver / Gold (планируется)

Нормализация, агрегации, витрины

Мониторинг и алерты качества данных

🛣 Roadmap
Санкционные списки (EU / UK)

dbt-модели Silver / Gold

BI-дашборды (Metabase / Superset)

Расширенные compliance-алерты

✔️ Итог
Проект демонстрирует системный подход к Data Engineering,
где ingestion, архитектура и требования к данным связаны
с реальными бизнес- и compliance-сценариями.

Репозиторий может использоваться как портфолио-проект
и база для дальнейшего развития.